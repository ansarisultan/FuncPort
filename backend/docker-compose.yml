version: '3.8'

services:
  mock-backend:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - CORS_ORIGIN=http://localhost:5173
    volumes:
      - ./data:/app/data
    restart: unless-stopped
